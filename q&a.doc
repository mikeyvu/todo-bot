1. How to config and connect a mongodb database?

2. What is async function and what to remember when using it? 

3. What is await function?

4. What is Mongodb schema and what is the format to define a model in schema?

5. What is middleware and what are the popular middelware nowadays? 

6. How do expressjs handle the PUT request? What is it different from other frameworks like Spring framework?

7. What is React Router and how is it different with traditional anchor tag on html page? 

8. How does React Router help with changing the pages on client website?

9. Explain this command: npm i reacter-router sonner axios lucide-react

10. Wrap the whole application in BrowserRouter for what?

11. What are the steps to install vite, tailwindcss and shadcn/ui?

12. Background generate using patterncraft.fun 

13. What is tailwind merge and how does it resolve merge conflict between tailwind layers?

14. What is useEffect and what does it track? (1:40:00)

15. Something is wrong with my github commits, where the backend successfully commited its message, while the frontend and the root are mixing up their commits

16. What is aggregate pipeline (phuong phap bang chuyen tong hop)? What is $facet in aggregate() function? 

17. What is onChange parameter? What is value parameter and how do they affect the input element?

18. What is arrow function? And how React parse the event into those functions?

19. What is onKeyPress parameter and how can we implement it?

20. Why did the database connection fail? And how did the code snippet below solve the problem?
        import dns from "node:dns";
        // ✅ Force Node.js to use Cloudflare DNS
        dns.setServers(["1.1.1.1", "8.8.8.8"]);

21. Why the database is connected but not getting data from the database? How to configure the db name in the URL String?

22. git submodule and tracked folder?