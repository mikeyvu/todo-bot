MONGODB_CONNECTIONSTRING=
PORT=5001
NODE_ENV=development

GOOGLE_CLIENT_ID=
# Not used by the current ID-token sign-in flow (no auth code is exchanged).
# Kept only in case a future server-side redirect flow needs it.
GOOGLE_CLIENT_SECRET=
JWT_SECRET=
